use BD_SICOOB

select * from teste

insert into teste values ('maria')

alter table celular ADD modstamp DATETIME2 NULL DEFAULT GETDATE()

alter table celular drop dataa

alter table teste
    add constraint dataa default (getdate()) for dataa;



CREATE TABLE teste(

id int Identity(1,1) Primary Key,
marca varchar (15),
modelo varchar (15),
condicao varchar (15),
valor int,
imei int,
dta DateTime Default getdate(),

)

insert into teste values ('samsumg', 'a02', 'novo', '600', '123456', '')

SELECT * from teste

SELECT GETDATE ()

SELECT dta, DATEADD(month, 1, SYSDATETIME()),

sp_help teste,

select convert (char, getdate(), 103)

use BD_SICOOB

SELECT * from teste

DELETE celular

DELETE FROM notebook WHERE id=7

SELECT CONVERT (date, GETDATE())



Create Table Clientes

(Codigo Int Identity(1,1) Primary Key,

 Nome Varchar(100) Not Null,

 DataCadastro DateTime Default GetDate())




CREATE DATABASE SISBD

use SISBD;

CREATE TABLE teste8(

id int Identity(1000,1) Primary Key,
imb varchar (25),
marca varchar (25),
modelo varchar (25),
condicao varchar (25),
anoFab varchar (25),
cfg varchar (25),
tela varchar (25),

preco varchar (25),
serviceTag varchar (25),
teamViewer varchar (25),
nomeRede varchar (25),
ssd varchar (25),
HDexp varchar (25),
carregador varchar (25),

processador varchar (25),
marcaPro varchar (25),
frequenciaPro varchar (25),
geracaoPro varchar (25),
ram varchar (25),
ddr varchar (25),
frequenciaMemo varchar (25),

anteVirus varchar (25),
memoExp varchar (25),
chaveW varchar (25),
chaveO varchar (25),
windows varchar (25),
office varchar (25),
descricao varchar (50),
data varchar (25)
)

CREATE TABLE celular(

id int Identity(2000,1) Primary Key,
Imei varchar (25),
Imei2 varchar (25),
marca varchar (25),
modelo varchar (25),
condicao varchar (25),
anoFab varchar (25),
cor varchar (25),
preco varchar (25),
memoria varchar (25),
processador varchar (25),
modeloPro varchar (25),
frequencia varchar (25),
ram varchar (25),
bateria varchar (25),
sistema varchar (25),
microSD varchar (25),
dual varchar (25),
chip1 varchar (25),
chip2 varchar (25),
numero1 varchar (25),
numero2 varchar (25),
data varchar (25)
)
